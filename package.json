{
  "name": "portsmouth-bin-days",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "npx playwright-core install --with-deps chromium",
    "start": "node server.mjs"
  },
  "dependencies": {
    "express": "5.0.0",
    "playwright-core": "^1.52.0"
  }
}
